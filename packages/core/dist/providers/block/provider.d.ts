import React from 'react';
interface StarknetBlockProviderProps {
    interval?: number;
    children: React.ReactNode;
}
export declare function StarknetBlockProvider({ interval, children, }: StarknetBlockProviderProps): JSX.Element;
export {};
