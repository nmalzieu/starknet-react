import React from 'react';
interface StarknetTransactionManagerProviderProps {
    children: React.ReactNode;
    interval?: number;
}
export declare function StarknetTransactionManagerProvider({ children, interval, }: StarknetTransactionManagerProviderProps): JSX.Element;
export {};
